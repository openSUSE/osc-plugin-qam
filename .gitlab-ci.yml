image: ibs:latest

test:
  script:
    - cd /builds/qa-maintenance/qam-oscplugin
    - zypper install --no-confirm m2crypto pycurl urlgrabber pytest mock prettytable
    - py.test tests
  tags:
    - qam
  only:
    - /^master.*$/
